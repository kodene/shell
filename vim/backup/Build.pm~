package Software::Build;

use Moo;
use WSB::Types qw( Str );
use namespace::clean;


has ['configure', 'make', 'test', 'install', 'clean'] => (
    is => 'rw',
    isa => Str,
    predicate => 1,
);

1;
