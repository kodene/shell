package Software::StateRole;

use Moo::Role;
use Types::Standard qw( Bool );
use WSB::Types qw( Datetime );

has 'enabled' => (
    is      => 'rw',
    isa     => Bool,
    default => sub { 1 },
);

# use Types::Standard qw( Bool );
# use WSB::Types -types;
# use Moo::Role;
# use namespace::clean;
#
# has 'enabled' => (
#     is      => 'rw',
#     isa     => Bool,
#     default => sub { 1 },
# );
#
# has 'installed' => (
#     is      => 'rw',
#     isa     => Bool,
#     default => sub { 0 },
# );
#
# has 'installed_on' => (
#     is          => 'rw',
#     isa         => Datetime,
#     coerce      => 1,
#     lazy_build  => 1,
# );

1;
