package WSB::Result;

use Moo;
use Types::Standard qw( Bool Str );
# use Log::Log4perl qw( get_logger );
# use Log::Log4perl::Level ();

has 'error' => (
    is      => 'ro',
    isa     => Bool,
    reader  => 'is_error',
    default => 0,
);

has 'message' => (
    is  => 'ro',
    isa => Str,
);

has 'data' => (
    is      => 'ro',
    default => '',
);

# sub BUILD {
#     my $self = shift;
#
#     if ( not Log::Log4perl->initialized() ) {
#         Log::Log4perl->easy_init( Log::Log4perl::Level::to_priority( 'OFF' ));
#     }
#
#     if ( $self->is_error ) {
#         get_logger()->warn( $self->message );
#     }
# }

1;
